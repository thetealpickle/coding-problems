// Created by Jessica Joseph on 4/12/18

#include <iostream>
#include <map>
#include <string>

using namespace std;

bool oneAway(string strOne, string strTwo) {
	map<char, int> container;
	for (auto element: strOne) {
		if (container.find(element)!= container.end()){
			container.insert(make_pair())
		}
	}

	for (suto element: strTwo){
	
	}
}

int main() {
	oneAway("pale", "ple");
}
